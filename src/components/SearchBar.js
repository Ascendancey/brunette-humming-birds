import React, { useState } from 'react';

import "./SearchBar.css";

const SearchBar = (props) => {

  return (
    <div className="">
        <p>{props.hi}</p>
        <input></input>
    </div>
  );
};

export default SearchBar;